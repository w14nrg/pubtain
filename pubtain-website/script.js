(()=>{const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const loader=$('.loader');const finish=()=>{document.body.classList.add('is-loaded');loader?.classList.add('is-hidden')};window.addEventListener('load',()=>setTimeout(finish,500));setTimeout(finish,2600);$('#year').textContent=new Date().getFullYear();const menu=$('#menu'),toggle=$('.menu-toggle');const close=()=>{document.body.classList.remove('menu-open');menu.classList.remove('is-open');menu.setAttribute('aria-hidden','true');toggle.setAttribute('aria-expanded','false')};toggle.addEventListener('click',()=>{const open=!menu.classList.contains('is-open');document.body.classList.toggle('menu-open',open);menu.classList.toggle('is-open',open);menu.setAttribute('aria-hidden',String(!open));toggle.setAttribute('aria-expanded',String(open))});$$('.menu a').forEach(a=>a.addEventListener('click',close));$('.menu__backdrop').addEventListener('click',close);
const steps=$$('.story__step'),frames=$$('.story__frame'),number=$('#scene-number');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const scene=Number(entry.target.dataset.scene);steps.forEach((el,i)=>el.classList.toggle('is-active',i===scene));frames.forEach((el,i)=>el.classList.toggle('is-active',i===scene));number.textContent=String(scene+1).padStart(2,'0')}),{rootMargin:'-35% 0px -45% 0px',threshold:0});steps.forEach(el=>observer.observe(el));
const rows=$$('.service-row'),images=$$('.service-image');const activate=i=>{rows.forEach((el,n)=>el.classList.toggle('is-active',n===i));images.forEach((el,n)=>el.classList.toggle('is-active',n===i))};rows.forEach((row,i)=>['mouseenter','focus','click'].forEach(event=>row.addEventListener(event,()=>activate(i))));
const targets=$$('.section-head,.cover-card,.mini-card,.process__steps article,.contact__lead,.contact-form');targets.forEach(el=>el.dataset.reveal='');const reveal=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');reveal.unobserve(entry.target)}}),{threshold:.12});targets.forEach(el=>reveal.observe(el));
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const items=$$('.parallax-media img');let ticking=false;const update=()=>{items.forEach(img=>{const rect=img.parentElement.getBoundingClientRect(),progress=(innerHeight-rect.top)/(innerHeight+rect.height),y=(progress-.5)*13;img.style.transform=`translate3d(0,${y}%,0) scale(1.05)`});ticking=false};addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});update()}
const cursor=$('.cursor');if(matchMedia('(pointer:fine)').matches){let x=0,y=0,cx=0,cy=0;addEventListener('mousemove',e=>{x=e.clientX;y=e.clientY;cursor.classList.add('is-visible')});const move=()=>{cx+=(x-cx)*.14;cy+=(y-cy)*.14;cursor.style.left=`${cx}px`;cursor.style.top=`${cy}px`;requestAnimationFrame(move)};move();$$('[data-cursor]').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.querySelector('span').textContent=el.dataset.cursor);el.addEventListener('mouseleave',()=>cursor.querySelector('span').textContent='View')})}
$$('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),dx=e.clientX-(r.left+r.width/2),dy=e.clientY-(r.top+r.height/2);el.style.transform=`translate(${dx*.12}px,${dy*.12}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
$('#contact-form').addEventListener('submit',event=>{event.preventDefault();const d=new FormData(event.currentTarget),subject=encodeURIComponent(`Pubtain enquiry — ${d.get('venue')}`),body=encodeURIComponent(`Name: ${d.get('name')}\nPub/company: ${d.get('venue')}\nEmail: ${d.get('email')}\nTelephone: ${d.get('phone')||'Not provided'}\n\nWhat they need help with:\n${d.get('message')}`);location.href=`mailto:hello@pubtain.com?subject=${subject}&body=${body}`})})();
